# Custom display widget source — disabled in favour of built-in ZMK widgets.
# Kept here for future reference. Re-enable with CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
# if (CONFIG_ZMK_DISPLAY)
#     target_sources(app PRIVATE src/custom_status_screen.c)
# endif()
